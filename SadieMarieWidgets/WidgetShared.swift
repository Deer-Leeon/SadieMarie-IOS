import Foundation

/// App group for sharing data between the admin app and widget extension (future use).
enum WidgetShared {
    static let appGroupIdentifier = "group.com.lj-buchmiller.SadieMarie"
}
