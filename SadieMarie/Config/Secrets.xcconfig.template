// Copy this file to `Secrets.xcconfig` and fill in values locally.
// `Secrets.xcconfig` is gitignored.
//
// Currently empty — the Clerk publishable key is hardcoded in
// `SadieMarieApp.swift`. Add new build-time configuration values
// here if/when you need to vary them per environment.
